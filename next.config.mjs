/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "jade-aspiring-termite.glitch.me"], // Add your Glitch app's domain here
  },
};

export default nextConfig;
